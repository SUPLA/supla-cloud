{% extends '@web/index.html' %}

{% block beforeVue %}
    <script>
        window.directLink = {{ directLink | json_encode | raw }};
        window.failureReason = {{ failureReason is defined ? (failureReason | json_encode | raw) : '""' }};
    </script>
{% endblock %}
