{% extends 'SuplaBundle::vue-template.html.twig' %}

{% block vue %}
    <router-view last-username="{{ last_username }}"
            error="{{ error }}"
            :ask-for-target-cloud="{{ askForTargetCloud ? 'true' : 'false' }}"
            last-target-cloud="{{ lastTargetCloud }}"></router-view>
{% endblock %}
